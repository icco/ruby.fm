<h1><%= @podcast.title %></h1>

<p>
  <strong>Notes:</strong>
  <%= @podcast.notes %>
</p>

<p>
  <strong>Published:</strong>
  <%= @podcast.published ? 'yes' : 'no' %>
</p>

<p>
  <strong>Audio:</strong>
  <audio controls>
    <source src="<%= @podcast.audio %>" type="audio/mpeg">
    Your browser does not support the audio element.
  </audio>
</p>

<%= link_to 'Edit', edit_podcast_path(@podcast.id) %> |
<%= link_to 'Back', channel_slug_path(@channel.slug) %>
