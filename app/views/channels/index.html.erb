<div class="center container">
  <h1 class="h1 mt4">Channels</h1>
  <hr>
</div>

<div class="clearfix px4">
  <% @channels.each do |channel| %>
    <%= render(partial: 'channels/channel', locals: { channel: channel }) %>
  <% end %>
</div>
