      <h1 class="h2 regular">You have <%= Channel.first.episodes.count %> episodes</h1>
      <ul>
        <% @episodes.each do |episode| %>
          <li><%= render(partial: 'episodes/episode', locals: { episode: episode, channel: episode.channel }) %></li>
        <% end %>
      </ul>
